import { GalaxyExperience } from "@/components/galaxy/GalaxyExperience";

export const metadata = { title: "Music Galaxy · VibeVerse" };

export default function GalaxyPage() {
  return <GalaxyExperience />;
}
